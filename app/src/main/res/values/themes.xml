<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.CopenhagenBuzz" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <!-- Primary app colors -->
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorSecondary">@color/bg_secondary</item>

        <item name="android:textColorPrimary">@color/text_primary</item>
    </style>

    <style name="Theme.CopenhagenBuzz" parent="Base.Theme.CopenhagenBuzz" />

    <!-- Used to make the text black in the calendar -->
    <style name="CustomDatePickerDialog" parent="Theme.AppCompat.Light.Dialog">
        <item name="android:textColorPrimary">@color/text_secondary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
        <item name="android:textColor">@color/text_secondary</item>
        <item name="android:buttonBarButtonStyle">@style/AcceptAndCancelText</item>
    </style>
    <!-- Custom style for the accept and cancel buttons in the date picker dialog -->
    <style name="AcceptAndCancelText" parent="Widget.AppCompat.Button.ButtonBar.AlertDialog">
        <item name="android:textColor">@color/text_secondary</item>
    </style>

        <!-- Custom style for dropdown list items -->
    <style name="CustomDropdownItem" parent="Widget.AppCompat.TextView">
        <item name="android:textColor">@color/text_secondary</item>
        <item name="android:background">@color/white</item>
    </style>
</resources>